# Dorm Life CDN Assets

Public, generated scene imagery for the dorm management system UI.

- Images are presentation assets only and contain no user data.
- Application code and private configuration remain in the private main repository.
- Consumers should pin jsDelivr URLs to a commit SHA for deterministic caching.

